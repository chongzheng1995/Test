# Create env python for this project like myvenv

# List of pip to install

pip install flask
pip install flask_sqlalchemy
pip install flask_login
pip install werkzeug
pip install pyotp
pip install hashlib
pip install opencv-python
pip install "dlib-19.22.99-cp310-cp310-win_amd64.whl"
pip install cmake
pip install face-recognition

## Open DB

1. go to sqlite local and create your app.db on this directory. for example
```
sqlite
.open C:/Users/chong/Desktop/Projects/Python/Test/app.db
```

https://pypi.org/project/face-recognition/